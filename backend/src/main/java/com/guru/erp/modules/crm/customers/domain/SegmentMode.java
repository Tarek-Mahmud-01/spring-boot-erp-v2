package com.guru.erp.modules.crm.customers.domain;

/**
 * ENT-073.mode — STATIC (manually curated member list) or DYNAMIC (rebuilt
 * from {@code definition} rules on demand), reference {@code SegmentMode}
 * (FR-220).
 */
public enum SegmentMode {
    STATIC,
    DYNAMIC
}
