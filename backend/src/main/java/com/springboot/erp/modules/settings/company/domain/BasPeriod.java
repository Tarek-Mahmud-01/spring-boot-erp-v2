package com.springboot.erp.modules.settings.company.domain;

/** AU compliance BAS lodgement cadence (quarterly is the ATO default). */
public enum BasPeriod {
    MONTHLY,
    QUARTERLY,
    ANNUAL
}
